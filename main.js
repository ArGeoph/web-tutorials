// //Add or remove background and borders if menu item is selected 
// document.querySelectorAll("nav > ul > li").addEventListener("click", toggleMenuButton, false);

// const toggleMenuButton = (event) => {
//     event.currentTarget.siblings
// };

let test = 400.0000;